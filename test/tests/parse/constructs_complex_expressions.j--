int test;

main() {
    int test2;

    if(test == test2) {
        print("They match");
    }

    boolean a;
    boolean b;

    if(a) {
        print("a");
    } else if(b) {
        print("b");
    } else {
        print("neither");
    }

    if(a && b) {
        print("both");
    }

    if(a || b) {
        print("one");
    }

    if(a && b || b && a) {
        test();
    }

    if(a || b && c && d) {
        test();
    }

    print("%d", 5 * 3 + 2);

    test = (1  + 5) * 3;
    test = -(5);
    test = -(-(-(5)));
    test = (-5);
    test = (1 * 4) + 3;
    test = (1 + 2) + 3;
    test = (5 - 3) * 3;
    test = (5 / 3) - 3;
    test = 5 / 3 * 3 - 3;
    test = 2 * 3 / 4 + 4;
    test = 2 * 3 % 4 / 5;
    test = 3 % 4 * 3 / 5;
    test = 4 / 5 * 3 % 2;
}
